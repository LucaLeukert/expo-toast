---
title: API (JSDoc)
description: Auto-generated from JSDoc comments in src/.
slug: reference/api
---

_This page is generated by `scripts/sync-api-docs.mjs`. Do not edit manually._

## Functions

### `normalizeDuration(duration: ToastDuration \| undefined, variant: ToastVariant): number`

Normalizes toast duration presets into milliseconds.

### `parseIOSMajorVersion(version: string \| number): number`

Parses an iOS runtime version into its major integer.

### `runtimeSupportsToastFor(os: string, _version: string \| number): boolean`

Returns whether the provided runtime tuple is eligible for native toast support checks.

## Objects

### `toast: ToastApi`

Imperative toast API for showing, updating, and dismissing toasts.

## Interfaces

### `interface ToastAction`

Action button configuration for a toast.

#### Members

- `label: string;` - Action button label.
- `onPress?: (event: ToastActionPressEvent) => void;` - Handler called when the action button is pressed.

### `interface ToastApi`

Public imperative API exposed as `toast`.

#### Members

- `show(options: ToastOptions): ToastId;` - Shows a toast with full control over content and behavior.
- `transition(id: ToastId, options: ToastTransitionOptions): ToastId;` - Transitions an existing toast by id.
- `update(id: ToastId, options: ToastTransitionOptions): ToastId;` - Alias for `transition`.
- `success(message: string, options?: ToastMessageOptions): ToastId;` - Shows a success toast.
- `error(message: string, options?: ToastMessageOptions): ToastId;` - Shows an error toast.
- `info(message: string, options?: ToastMessageOptions): ToastId;` - Shows an info toast.
- `loading(message: string, options?: ToastMessageOptions): ToastId;` - Shows a loading toast.
- `promise<T>( work: Promise<T>, messages: ToastPromiseMessages<T>, options?: ToastPromiseOptions, ): Promise<T>;` - Binds toast state to a promise lifecycle.
- `configure(config: ToastConfig): void;` - Updates global runtime defaults.
- `dismiss(id?: ToastId): void;` - Dismisses one toast by id, or all toasts when id is omitted.
- `dismissAll(): void;` - Dismisses all visible and queued toasts.
- `isSupported(): boolean;` - Returns whether native runtime support is currently available.

### `interface ToastConfig`

Global runtime defaults and queue controls.

#### Members

- `duration?: ToastDuration;` - Default duration for new toasts.
- `position?: ToastPosition;` - Default render position.
- `size?: ToastSize;` - Default width behavior.
- `haptics?: boolean;` - Default haptics behavior.
- `announce?: boolean;` - Default announcement behavior.
- `importance?: ToastImportance;` - Default accessibility importance.
- `motion?: ToastMotionPreference;` - Default motion behavior.
- `dedupeWindowMs?: number;` - Time window used to dedupe matching toasts in milliseconds.
- `maxVisible?: number;` - Maximum simultaneously visible toasts per edge.
- `maxQueue?: number;` - Maximum queued pending toasts per edge.
- `dropPolicy?: ToastDropPolicy;` - Queue eviction strategy when `maxQueue` is exceeded.

### `interface ToastOptions`

Full options used when showing a toast.

#### Members

- `id?: ToastId;` - Explicit id to use for the toast. If omitted, an id is generated.
- `variant?: ToastVariant;` - Visual style preset. Defaults to `'info'`.
- `title?: string;` - Optional title text shown above `message`.
- `message: string;` - Main toast body text.
- `action?: ToastAction;` - Optional action button.
- `duration?: ToastDuration;` - Display duration in milliseconds or semantic preset.
- `position?: ToastPosition;` - Screen edge used for rendering. Falls back to global config.
- `size?: ToastSize;` - Width behavior for this toast. Falls back to global config.
- `haptics?: boolean;` - Enables haptic feedback when showing the toast.
- `dedupeKey?: string;` - Key used for dedupe matching within `dedupeWindowMs`.
- `accessibilityLabel?: string;` - Accessibility label override announced by assistive technologies.
- `announce?: boolean;` - Whether this toast should trigger an accessibility announcement.
- `importance?: ToastImportance;` - Accessibility importance for announcement behavior.
- `motion?: ToastMotionPreference;` - Motion behavior override for this toast.
- `onShow?: (event: ToastShowEvent) => void;` - Called when the toast is shown.
- `onDismiss?: (event: ToastDismissEvent) => void;` - Called when the toast is dismissed.

### `interface ToastPromiseMessages`

Text messages used by `toast.promise`.

#### Members

- `loading: string;` - Message shown while the promise is pending.
- `success: string \| ((value: T) => string);` - Success message or mapper using the resolved value.
- `error: string \| ((error: unknown) => string);` - Error message or mapper using the rejection reason.

### `interface ToastPromiseOptions`

Optional per-state options for `toast.promise`.

#### Members

- `loading?: ToastMessageOptions;` - Extra options for the loading toast.
- `success?: ToastMessageOptions;` - Extra options for the success transition.
- `error?: ToastMessageOptions;` - Extra options for the error transition.

### `interface ToastTransitionOptions`

Options used to update an existing toast.

#### Members

- `variant?: ToastVariant;` - New variant to apply.
- `title?: string;` - New title text.
- `message?: string;` - New body text.
- `action?: ToastAction \| null;` - Action button update.
Use `null` to clear the current action.
- `duration?: ToastDuration;` - New duration in milliseconds or semantic preset.
- `size?: ToastSize;` - New width behavior.
- `haptics?: boolean;` - Haptics override for this transition.
- `accessibilityLabel?: string;` - Accessibility label override.
- `announce?: boolean;` - Announcement behavior override.
- `importance?: ToastImportance;` - Accessibility importance override.
- `motion?: ToastMotionPreference;` - Motion behavior override.
- `onDismiss?: (event: ToastDismissEvent) => void;` - Dismiss callback replacement for this toast id.

## Type Aliases

### `type ExpoToastModuleEvents = { onToastShow: (event: ToastShowEvent) => void; onToastDismiss: (event: ToastDismissEvent) => void; onToastActionPress: (event: ToastActionPressEvent) => void; }`

Native module event callbacks exposed by `expo-toast`.

### `type ToastActionPressEvent = { id: ToastId; variant: ToastVariant; title: string \| null; message: string; position: ToastPosition; }`

Event emitted when a toast action button is pressed.

### `type ToastDismissEvent = { id: ToastId; reason: ToastDismissReason; }`

Event emitted when a toast is dismissed.

### `type ToastDismissReason = 'timeout' \| 'swipe' \| 'programmatic' \| 'replaced'`

Reason the toast left the screen.

### `type ToastDropPolicy = 'oldest' \| 'newest'`

Queue eviction strategy when pending queue capacity is reached.

### `type ToastDuration = number \| 'short' \| 'long' \| 'infinite'`

Display duration in milliseconds or a semantic preset.

### `type ToastId = string`

Unique identifier for a toast instance.

### `type ToastImportance = 'low' \| 'normal' \| 'high'`

Accessibility/announcement priority for a toast.

### `type ToastMessageOptions = Omit<ToastOptions, 'message' \| 'variant'>`

Shared toast options for convenience APIs that provide message/variant separately.

### `type ToastMotionPreference = 'system' \| 'full' \| 'minimal'`

Motion behavior preference.

### `type ToastPosition = 'top' \| 'bottom'`

Screen edge where a toast stack is rendered.

### `type ToastShowEvent = { id: ToastId; }`

Event emitted when a toast is presented.

### `type ToastSize = 'fit-content' \| 'fill-width'`

Width behavior for a toast.

### `type ToastVariant = 'success' \| 'error' \| 'info' \| 'loading'`

Visual style preset for a toast.
