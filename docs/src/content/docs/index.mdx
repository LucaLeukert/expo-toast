---
title: Overview
description: Expo toast notifications with native iOS presenter, queueing, and polished motion.
template: splash
hero:
  tagline: Native toast notifications for Expo
  actions:
    - text: Quick Start
      link: /guides/quick-start
    - text: API Reference
      link: /guides/api
---

<div class="expressive-grid">
  <div class="expressive-card">
    <strong>Native iOS Presenter</strong><br />
    Liquid-glass visuals, queueing, and swipe dismissal built in Swift.
  </div>
  <div class="expressive-card">
    <strong>DX APIs</strong><br />
    `toast.promise`, `toast.configure`, and `toast.update` for ergonomic app flows.
  </div>
  <div class="expressive-card">
    <strong>A11y + Motion</strong><br />
    Accessibility labels, announce/importance controls, and reduced-motion awareness.
  </div>
</div>

## Why this module

`expo-toast` gives Expo apps a deterministic, typed toast API while using a native iOS presenter for high-quality animations and interaction fidelity.

## Canonical Docs Policy

This docs site includes generated references synced from repository root files:

- `src/**` public JSDoc API comments
- `README.md`
- `CHANGELOG.md`

Run `bun run docs:sync` after editing either file.
